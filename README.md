# Marketing Leo

Static one-page marketing site.

## Structure

- `index.html` - page markup and sections
- `css/` - compiled Bootstrap and custom styles
- `js/` - custom page behavior
- `img/` - images used by the site
- `lib/` - minified third-party plugins used by `index.html`
